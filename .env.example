# App ports exposed from the Caddy frontend container
APP_HTTP_PORT=8080
APP_HTTPS_PORT=8443

# Postgres container credentials
POSTGRES_DB=mealmode
POSTGRES_USER=mealmode
POSTGRES_PASSWORD=change-me

# Backend Django DB connection (should match Postgres settings above)
USE_POSTGRES=true
DB_HOST=db
DB_PORT=5432
DB_NAME=mealmode
DB_USER=mealmode
DB_PASSWORD=change-me

# Backend runtime tuning
GUNICORN_WORKERS=4
